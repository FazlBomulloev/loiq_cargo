# ТЗ: Система управления оптовым карго (Китай → Душанбе)

## 1. Цель и суть

Веб-система для карго, работающего с оптовиками. Ведёт товары от
складов в Китае до выдачи клиенту в Душанбе, считает стоимость по
плотности, помогает оптимально грузить фуры и даёт аналитику. Состоит
из сайта с личными кабинетами (клиент / сотрудники / овнер) и
телеграм-бота для уведомлений.

**Первый шаг работы — не код, а дизайн.** Claude Code сначала находит
в интернете референсы минималистичного интерфейса (admin-панели,
логистические дашборды), предлагает 2–3 направления с примерами,
согласует со мной, и только после одобрения начинает разработку.

---

## 2. Технологический стек

- Backend: FastAPI (async)
- ORM: SQLAlchemy (async) + Alembic (миграции)
- БД: PostgreSQL
- Кэш / очереди: Redis
- Планировщик: APScheduler (начисление простоя, пометка «горящих»)
- Бот: aiogram 3
- Frontend: React + TypeScript
- Контейнеризация: Docker (docker-compose)

---

## 3. Роли и права

| Роль | Доступ |
|---|---|
| **Клиент** | Свои товары, статусы, статистика, калькулятор. Только чтение + уведомления |
| **Сотрудник склада Китая** (×3) | Только свой склад: приёмка, отправка фуры, заявки на изменение |
| **Сотрудник Душанбе** | Приёмка партий (накладные), выдача, калькулятор |
| **Овнер** | Всё по всем складам: аналитика, тарифы, настройки, сотрудники, клиенты, обработка заявок, привязка «без клиента» |

Склады Китая независимы, у каждого один сотрудник, видящий только
свои товары. Склады: **Иу, Урумчи, Кашгар**. Пункт назначения — Душанбе.

---

## 4. Сущности и модель данных

Ключевые сущности (поля — основные, не исчерпывающе):

- **Client** — id, client_code (напр. LQ007), ФИО/название, телефон,
  telegram_chat_id, дата регистрации, статус верификации.
- **Warehouse** — id, название (Иу/Урумчи/Кашгар), тариф (ссылка),
  лимиты фуры (объём/вес, дефолт).
- **User (staff)** — id, роль, привязка к складу (для China-роли).
- **Goods (товар)** — id, client_id, warehouse_id, описание
  (опционально), вес (брутто, кг), объём (м³), плотность (вычисляется),
  дата прихода в Китай, статус, партия_id (nullable), признаки
  «горящий», флаг «без клиента».
  *Один приём = один товар = одна запись.* Идентификация — код
  клиента + партия (см. §14, открытый вопрос по нумерации).
- **Партия (Shipment)** — id, warehouse_id, дата отправки, список
  товаров, суммарный объём/вес/стоимость, статус.
- **Tariff** — id, warehouse_id, набор строк плотностной сетки.
- **TariffRow** — плотность_от, плотность_до, ставка $/кг.
- **ChangeRequest (заявка)** — id, автор, товар/действие, статус
  (на рассмотрении / применено / отклонено).
- **Settings** — глобальные и по складам (N дней, порог заполненности,
  целевая стоимость, квоты, коэффициент простоя, дни бесплатного
  хранения, курс валют).

---

## 5. Статусы товара и жизненный цикл

`на складе Китая` → `в пути` → `в Душанбе` → `выдан`

Плюс служебные флаги: `без клиента` (неизвестный код при приёмке),
`недостача` (в накладной есть, физически не доехал), `горящий`
(старше N дней).

Смена статуса триггерит уведомление клиенту (см. §12).

---

## 6. Модуль тарификации (по плотности)

Тариф у каждого склада свой. Считается по **плотности** груза:

1. Плотность = вес (брутто) ÷ объём (замер при приёмке), кг/м³.
2. По плотности берётся ставка $/кг из сетки склада.
3. Стоимость = ставка × вес.

Стартовая сетка (Кашгар; другие склады масштабируются по расстоянию,
пока ×1.1 Урумчи, ×1.3 Иу — вынести в настройки, откалибровать позже):

| Плотность, кг/м³ | Ставка $/кг |
|---|---|
| ≥ 250 | 0.70 |
| 150–249 | 1.30 |
| 100–149 | 2.50 |
| 50–99 | 3.00 |
| < 50 | 4.00 |

В ставку включена растаможка. Валюта расчётов с клиентом — сомони
(ставки в сетке храним в $, пересчёт по курсу из настроек). Сетка
редактируется овнером.

---

## 7. Калькулятор стоимости

- Доступен всем ролям **и публично на сайте без входа**.
- Вход: склад + объём + вес. Выход: примерная стоимость доставки
  (только фрахт, без простоя).
- Использует плотностную сетку выбранного склада.

---

## 8. Приёмка на складе Китая

- Сотрудник вводит код клиента → система подтверждает клиента →
  описание (опц.), объём, вес → сохраняет. Товар появляется у клиента,
  статус `на складе Китая`, уходит уведомление.
- **Неизвестный/ошибочный код** → товар создаётся с флагом
  `без клиента`, приёмка не блокируется; позже овнер привязывает к
  реальному клиенту.
- Сотрудник **не может** править/удалять свой приём напрямую —
  оставляет заявку (§13). Овнер применяет.

---

## 9. Оптимизатор загрузки фуры («Отправка»)

Кнопка «Отправка» на складе Китая. Алгоритм — жадный с квотами
(тяжёлые солверы не нужны). Порядок:

1. **Обязательные первыми.** Все `горящие` (старше N дней) и
   засидевшееся мелкое грузятся в фуру до оптимизации.
2. **Добор по квотам плотности.** Свободный объём делится между
   группами: плотное (≥250), среднее (100–249), лёгкое (<100).
   Пропорция настраивается (старт 40 / 35 / 25 % объёма).
3. **Внутри каждой группы — по старшинству** (кто дольше лежит).
4. **Топ-ап остатком**, чтобы не ушла полупустой.
5. **Проверка трёх ворот:**
   - заполненность ≥ порога (по объёму ИЛИ весу, старт 90%);
   - стоимость ≥ целевой (старт $27k);
   - нет перегруза по весу (≤ лимита фуры).
6. **Показ сотруднику** с флагами («перегруз по весу», «недобор до
   цели», «заполнена на X%») и **ручной правкой** (добавить / убрать /
   заменить).

При подтверждении: товары → `в пути`, создаётся партия, лимиты фуры
берутся из склада (можно переопределить в момент отправки).

Целевая стоимость $27k — «ворота полной фуры» (не гнать полупустую /
невыгодную), а не жёсткий денежный минимум.

---

## 10. Приёмка в Душанбе (накладная)

- После «отправлено» партия автоматически появляется у сотрудника
  Душанбе как **накладная** (список товаров: код клиента, описание,
  вес, объём, дата прихода в Китай).
- Сотрудник идёт по списку и отмечает **каждый товар** «принято».
- Товар из накладной, который не доехал, — не отмечается → флаг
  `недостача`, статус остаётся `в пути` для разбора.
- Кнопка «Принять партию» → все отмеченные товары → `в Душанбе`,
  клиентам уходят уведомления, по каждому запускается счётчик простоя.

---

## 11. Выдача

- Только **полная выдача**: клиент забирает все свои товары со статусом
  `в Душанбе` разом.
- Поиск по коду клиента → выдать → статус `выдан`.
- При выдаче показывается сумма к оплате = фрахт + простой (§12).
  Оплата фиксируется (оплачено / долг).

---

## 12. Простой (хранение)

- Первые 10 дней после прихода в Душанбе — бесплатно.
- Далее за каждый день добавляется плата (коэффициент — в настройки,
  сумму задаёт овнер).
- Начисление — ежедневная задача APScheduler.
- Сумма простоя входит в оплату при выдаче.

---

## 13. Заявки на изменение

- Сотрудники складов не редактируют/не удаляют приёмы напрямую.
- Создают заявку → овнер видит список, применяет или отклоняет.
- Овнер редактирует напрямую.

---

## 14. Телеграм-бот (aiogram 3)

**Регистрация-верификация.** Клиент регистрируется на сайте, система
выдаёт код. Привязка телеграма:
- то же устройство, что и ТГ → deep-link `t.me/bot?start=<код>`
  (привязка автоматически);
- другое устройство → ручной ввод кода в боте.
После привязки сохраняется `telegram_chat_id`.

**Уведомления (только они, бот не интерактивный).** По каждому товару,
на трёх событиях:
- прибыл на склад Китая;
- отправлен (в пути);
- прибыл в Душанбе.

Формат: «Ваш товар от 28 августа прибыл в Китай, посуда 150 кг, 1 куб».

---

## 15. Дашборд и аналитика

- **Овнер:** выручка по партиям / складам / за период, объёмы,
  активные товары, что копит простой, недостачи, заявки.
- **Клиент:** своя статистика — сколько привёз, что в пути, что ждёт
  выдачи, что на простое.

---

## 16. Настройки овнера

Всё вынести в редактируемые настройки со значениями по умолчанию:
- тарифные сетки по складам, коэффициенты складов, курс валют;
- лимиты фур (объём/вес) по складам;
- N дней «горящего» товара (дефолт 20);
- порог заполненности (дефолт 90%);
- целевая стоимость фуры (дефолт $27k);
- квоты плотности (дефолт 40/35/25);
- дни бесплатного хранения (дефолт 10) и коэффициент простоя.

---

## 17. Этап дизайна (делаем ПЕРВЫМ)

1. Claude Code ищет в интернете референсы **минималистичного**
   интерфейса под логистику/админку.
2. Готовит 2–3 направления (цвет, типографика, layout) с примерами.
3. Согласовывает со мной.
4. Только после одобрения — вёрстка и разработка.

Стиль: минимализм, чисто, без визуального шума, акцент на таблицах и
статусах.

---

## 18. Стандарты кода

- Без README-файлов.
- Обсуждение подхода до написания кода.
- Чистый код, минимум комментариев и докстрингов.
- Все импорты в начале файла.
- Обработка ошибок — конкретные `except` (без голого `except`/`pass`).
- Длина строки ≤ 79 символов.
- Все логи — на русском.

---

## 19. Открытые / отложенные вопросы

Заложить как настройки/заглушки, уточнить позже:

- **Нумерация товаров** клиента (несколько приходов под одним кодом) —
  пока оставляем как решено ранее (код клиента + партия, товар — это
  запись со своей датой приёмки). Уникальную клиентскую нумерацию
  (LQ007-1, -2…) — обсудим отдельно.
- Калибровка тарифов реальными ставками и коэффициентов складов.
- Скидки / программа лояльности — на будущее.
- Точная сумма простоя за день.
- $27k как ориентир, не жёсткий порог.
- Обработка непринятого товара (`недостача`).

---

## 20. Предлагаемые этапы разработки

1. Дизайн: референсы → согласование.
2. Каркас: БД, миграции, роли/авторизация, Docker.
3. Клиент: регистрация + верификация ТГ, кабинет, калькулятор.
4. Склад Китая: приёмка, статусы, уведомления.
5. Оптимизатор «Отправка» + партии.
6. Душанбе: накладные, приёмка, выдача.
7. Тариф + простой + оплата.
8. Заявки, привязка «без клиента».
9. Дашборды и аналитика.
10. Настройки овнера.
